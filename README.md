# ds_copilot-agent-demo

A simple command-line calculator written in Python. It supports addition, subtraction, multiplication, and division, including a check that prevents division by zero.

## Usage

Run the calculator with:

```bash
python main.py
```

Enter `q` at any prompt to quit.

## Requirements

- Python 3.8 or newer

The project uses only Python's standard library, so `requirements.txt` is empty.
